# nfra itemId=4291 第二入口 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 支持 nfra 列表采集第二入口 itemId=4291，与 4110 并列，采集流程/抽取方式完全一致，两次独立运行。

**Architecture:** 无爬虫代码改动——`run_crawl(item_id=...)` 及下游已全链路参数化，4110 仅是默认值。本任务只加 Makefile 便捷目标、README 文档说明，并做 itemId=4291 的端到端 smoke 验收。

**Tech Stack:** Make · Markdown · 现有 Python 爬虫（不动）

## Global Constraints

- 不改 `src/web_scraper_service/**`、`scripts/crawl_nfra.py`、测试——均不动。
- 两次独立运行 = 两次独立进程调用，浏览器/cookie 天然隔离；保持单 item_id 接口。
- djg_data 共表，doc_id 全局唯一（ON CONFLICT (doc_id,person_name) DO NOTHING），4110 与 4291 的 doc_id 不会冲突。
- venv 在 `.venv`；命令用 `.venv/bin/python`、`.venv/bin/pytest`。
- 约束引自 spec：`docs/superpowers/specs/2026-06-25-nfra-itemid-4291-design.md`。

## File Structure

| 文件 | 职责 |
|------|------|
| `Makefile` | 新增 `crawl-nfra-4291` target |
| `README.md` | 新增 nfra 文档采集章节（两个 itemId 入口） |
| `README.zh-CN.md` | 同上（中文） |

---

### Task 1: Makefile crawl-nfra-4291 target + README 文档

**Files:**
- Modify: `Makefile`（`crawl-nfra` target 之后追加 `crawl-nfra-4291`）
- Modify: `README.md`（`## Local Database` 章节之后插入 `## nfra Document Crawling` 章节）
- Modify: `README.zh-CN.md`（`## 本地数据库` 章节之后插入 `## nfra 文档采集` 章节）

**Interfaces:**
- Consumes: 现有 `scripts/crawl_nfra.py --item-id` 参数、`make crawl-nfra` target
- Produces: `make crawl-nfra-4291` target；README 两个入口说明

- [ ] **Step 1: Makefile 追加 crawl-nfra-4291 target**

在 `Makefile` 第 50 行 `crawl-nfra` target 的 recipe 之后追加（注意 recipe 行首是 TAB）：

```make

crawl-nfra-4291:
	uv run python scripts/crawl_nfra.py --pages $(or ${NFRA_PAGES},5) --item-id 4291
```

- [ ] **Step 2: 验证 make target 语法**

Run: `make -n crawl-nfra-4291`
Expected: 打印 `uv run python scripts/crawl_nfra.py --pages 5 --item-id 4291`（`-n` 干跑不执行）

Run: `make -n crawl-nfra`
Expected: 仍打印 `--item-id 4110`（原 target 未受影响）

- [ ] **Step 3: README.md 插入 nfra 文档采集章节**

在 `README.md` 的 `## Local Database (PostgreSQL + Redis only)` 章节结束之后、`## Docker Compose` 之前插入：

```markdown
## nfra Document Crawling

Crawl 国家金融监督管理总局 (nfra.gov.cn) 任职资格批复 into `zbd_crawler_data.djg_data`:

| itemId | 栏目 | make target |
|--------|------|-------------|
| 4110 | 总局机关 | `make crawl-nfra` |
| 4291 | (second entry) | `make crawl-nfra-4291` |

```bash
# default: itemId=4110
make crawl-nfra

# itemId=4291
make crawl-nfra-4291

# custom itemId / pages via env
NFRA_ITEM_ID=4291 NFRA_PAGES=3 make crawl-nfra
```

Detail pages are opened with a browser (DynamicFetcher) and fields extracted via Bailian LLM (`qwen3.5-35b-a3b`); requires `DASHSCOPE_API_KEY` in `.env`. Results skip already-stored doc_ids on rerun.
```

- [ ] **Step 4: README.zh-CN.md 插入 nfra 文档采集章节**

在 `README.zh-CN.md` 的 `## 本地数据库（仅 PostgreSQL + Redis）` 章节结束之后、`## Docker Compose` 之前插入：

```markdown
## nfra 文档采集

采集国家金融监督管理总局（nfra.gov.cn）任职资格批复，写入 `zbd_crawler_data.djg_data`：

| itemId | 栏目 | make target |
|--------|------|-------------|
| 4110 | 总局机关 | `make crawl-nfra` |
| 4291 | 第二入口 | `make crawl-nfra-4291` |

```bash
# 默认 itemId=4110
make crawl-nfra

# itemId=4291
make crawl-nfra-4291

# 通过环境变量自定义 itemId / 页数
NFRA_ITEM_ID=4291 NFRA_PAGES=3 make crawl-nfra
```

详情页用浏览器（DynamicFetcher）打开，字段经百炼 LLM（`qwen3.5-35b-a3b`）抽取；需在 `.env` 配置 `DASHSCOPE_API_KEY`。重复运行会跳过已存在的 doc_id。
```

- [ ] **Step 5: Commit**

```bash
git add Makefile README.md README.zh-CN.md
git commit -m "docs: add nfra itemId=4291 entry point (make target + README)"
```

---

### Task 2: itemId=4291 端到端 smoke 验收

**Files:** 无（仅运行验证）

**前置：** `DASHSCOPE_API_KEY` 已在 `.env`；Docker DB 在跑（`docker compose -f docker-compose.dev.yml up -d`）且 `zbd_crawler_data` 库存在；patchright chromium 已装。

- [ ] **Step 1: 小规模实跑 itemId=4291（1 页）**

Run: `timeout 500 make crawl-nfra-4291 NFRA_PAGES=1`
Expected: 日志依次出现「列表第 1 页获得 18 条」「标题含「任职资格」 N 条」「待抓取 K 个」「抽取行数 X」「写入 djg_data X 行」；退出码 0

- [ ] **Step 2: 验证 djg_data 落库（4291 的 doc_id）**

Run:
```bash
.venv/bin/python - <<'EOF'
import asyncio, asyncpg
async def main():
    conn = await asyncpg.connect("postgresql://scraper:scraper_secret@localhost:5432/zbd_crawler_data")
    cnt = await conn.fetchval("SELECT count(*) FROM djg_data")
    print("djg_data total rows:", cnt)
    for r in await conn.fetch("SELECT doc_id, person_name, position, institution_name FROM djg_data ORDER BY doc_id DESC LIMIT 5"):
        print(" ", dict(r))
    await conn.close()
asyncio.run(main())
EOF
```
Expected: 行数较 4110 smoke 后增加；新行 doc_id 来自 4291 栏目（如分局批复），person_name/position 合理

- [ ] **Step 3: 验证 skip 语义（重跑 4291）**

Run: `timeout 200 make crawl-nfra-4291 NFRA_PAGES=1`
Expected: 日志「待抓取 0 个」（已存在过滤），stored=0；djg_data 行数不变

- [ ] **Step 4: 全量测试套件无回归**

Run: `.venv/bin/python -m pytest -q`
Expected: 全 PASS（29/29，既有）

- [ ] **Step 5: 确认 4110 入口未受影响（可选）**

Run: `make -n crawl-nfra`
Expected: 打印 `--item-id 4110`（默认未变）

- [ ] **Step 6: Commit（如有 smoke 过程中的小修）**

```bash
git add -A
git commit -m "test: verify nfra itemId=4291 smoke run" || echo "nothing to commit"
```

---

## Self-Review 记录

- **Spec 覆盖**：无代码改动(spec §1)✓；Makefile crawl-nfra-4291(Task1)✓；README 两 itemId 入口(Task1)✓；4291 端到端 smoke(Task2)✓；skip 语义(Task2)✓；不改动爬虫/测试(spec §不改动)✓。
- **类型一致**：本计划无新代码接口，仅 Makefile/README 文本与运行命令，无跨任务类型依赖。
- **无占位符**：所有步骤含确切命令与预期输出；Makefile/README 内容完整内联。
