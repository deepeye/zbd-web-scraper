# 文档同步 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补齐 `.env.example`、`.env.docker.example` 和 `docs/DEPLOY.md` 中缺失的 capital/equity 配置项和端点说明。

**Architecture:** 纯文档编辑，三个文件各改一处，无依赖关系。

**Tech Stack:** 无

## Global Constraints

- 仅补齐缺失内容，不修改现有结构和描述
- 不新增文件，仅修改已有文件

---

### Task 1: 补齐 `.env.example` 中缺失的 env var

**Files:**
- Modify: `.env.example:90`

**Interfaces:**
- Produces: 无（独立文件）

- [x] **Step 1: 在 NFRA_SCHEDULE_PAGES=5 后追加两行**

在 `.env.example` 第 90 行 `NFRA_SCHEDULE_PAGES=5` 后插入：

```bash
NFRA_CAPITAL_SCHEDULE_ENABLED=true
NFRA_EQUITY_SCHEDULE_ENABLED=true
```

- [x] **Step 2: 验证**

```bash
grep "NFRA_CAPITAL_SCHEDULE_ENABLED\|NFRA_EQUITY_SCHEDULE_ENABLED" .env.example
```

预期：输出两行，显示两个变量均设为 `true`。

- [x] **Step 3: Commit**

```bash
git add .env.example
git commit -m "docs: add missing NFRA_CAPITAL/EQUITY_SCHEDULE_ENABLED to .env.example"
```

---

### Task 2: 补齐 `.env.docker.example` 中缺失的 env var

**Files:**
- Modify: `.env.docker.example:33`

**Interfaces:**
- Produces: 无（独立文件）

- [x] **Step 1: 在 NFRA_SCHEDULE_PAGES=5 后追加两行**

在 `.env.docker.example` 第 33 行 `NFRA_SCHEDULE_PAGES=5` 后插入：

```bash
NFRA_CAPITAL_SCHEDULE_ENABLED=true
NFRA_EQUITY_SCHEDULE_ENABLED=true
```

- [x] **Step 2: 验证**

```bash
grep "NFRA_CAPITAL_SCHEDULE_ENABLED\|NFRA_EQUITY_SCHEDULE_ENABLED" .env.docker.example
```

预期：输出两行，显示两个变量均设为 `true`。

- [x] **Step 3: Commit**

```bash
git add .env.docker.example
git commit -m "docs: add missing NFRA_CAPITAL/EQUITY_SCHEDULE_ENABLED to .env.docker.example"
```

---

### Task 3: 更新 `docs/DEPLOY.md` 补充 capital/equity 端点

**Files:**
- Modify: `docs/DEPLOY.md:48-55`

**Interfaces:**
- Produces: 无（独立文件）

- [x] **Step 1: 在手动触发采集段补充 capital/equity curl 示例**

在 `docs/DEPLOY.md` 第 52 行（现有 nfra crawl 轮询命令）之后，第 54 行（定时说明）之前，插入：

```bash
  # 注册资本/开业采集
  curl -X POST localhost:8000/api/v1/nfra/capital/crawl \
    -H "X-API-Key: $API_KEY" -H "Content-Type: application/json" \
    -d '{"pages": 5}'
  # 股权变更采集
  curl -X POST localhost:8000/api/v1/nfra/equity/crawl \
    -H "X-API-Key: $API_KEY" -H "Content-Type: application/json" \
    -d '{"pages": 5}'
```

- [x] **Step 2: 在定时说明段补充单独关闭开关的说明**

在第 54 行 `NFRA_SCHEDULE_ENABLED=false` 关闭。` 后追加：

```
`NFRA_CAPITAL_SCHEDULE_ENABLED=false` / `NFRA_EQUITY_SCHEDULE_ENABLED=false` 可单独关闭各类采集。
```

- [x] **Step 3: 在查询数据段补充 capital/equity 查询路径**

在第 55 行 `GET /api/v1/nfra/data` 说明后，追加：

```bash
- **查询注册资本/开业数据**：`GET /api/v1/nfra/capital/data?start_date=...&end_date=...`（见 `docs/API.md`）。
- **查询股权变更数据**：`GET /api/v1/nfra/equity/data?start_date=...&end_date=...`（见 `docs/API.md`）。
```

- [x] **Step 4: 验证**

```bash
grep "capital/crawl\|equity/crawl\|capital/data\|equity/data\|NFRA_CAPITAL_SCHEDULE\|NFRA_EQUITY_SCHEDULE" docs/DEPLOY.md
```

预期：输出包含 capital/equity 的 crawl 和数据查询路径，以及两个 env var 名称。

- [x] **Step 5: Commit**

```bash
git add docs/DEPLOY.md
git commit -m "docs: add capital/equity crawl endpoints and env vars to DEPLOY.md"
```